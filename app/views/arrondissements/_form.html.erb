
<%= simple_form_for(@arrondissement) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :vil, prompt: "Selectionner une ville" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
